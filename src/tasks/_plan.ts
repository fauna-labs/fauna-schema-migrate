import chalk from "chalk";
import { writeNewMigrationDir } from "../util/files";

// Not used atm. It might be useful for users to write the plan of an apply to a file.
// Instead of running the apply logic again in production it makes more sense and has less risk
// to apply an existing plan. Reach out if this makes sense/is necessary.
const planApply = async () => {

};

export default planApply;
